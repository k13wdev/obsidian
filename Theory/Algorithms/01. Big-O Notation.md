# Big-O Notation

## Суть и контекст

Big-O — это метрика и язык для описания эффективности алгоритмов. Вместо измерения скорости в секундах описывает, как количество операций растёт относительно размера входных данных (n). Big-O характеризует **скорость роста** (rate of increase) времени работы алгоритма.

## Ключевые идеи

- Big-O описывает **верхнюю границу** (upper bound) — гарантию наихудшего случая
- Константы и незначительные члены отбрасываются
- Для одного алгоритма существует три случая: наилучший, наихудший, средний
- Временная и пространственная сложности — разные характеристики
- Рекурсивные алгоритмы имеют особую формулу расчёта

## Определения и термины

**Big-O (O-большое)** — множество функций O(g(n)) = {f(n): существуют положительные константы c и n₀, такие что 0 ≤ f(n) ≤ c·g(n) для всех n ≥ n₀}. Описывает асимптотическую верхнюю границу. В индустрии O сливается с Θ и используется для описания максимально строгой/точной оценки (tight bound).

**Θ-нотация (Big Theta, асимптотически точная оценка)** — множество функций Θ(g(n)) = {f(n): существуют положительные константы c₁, c₂ и n₀, такие что 0 ≤ c₁·g(n) ≤ f(n) ≤ c₂·g(n) для всех n ≥ n₀}. Функция f(n) равна g(n) с точностью до константного множителя. f(n) = Θ(g(n)) тогда и только тогда, когда f(n) = O(g(n)) и f(n) = Ω(g(n)).

**Ω-нотация (Big Omega, асимптотическая нижняя оценка)** — множество функций Ω(g(n)) = {f(n): существуют положительные константы c и n₀, такие что 0 ≤ c·g(n) ≤ f(n) для всех n ≥ n₀}. Используется для описания нижней границы.

**o-нотация (Little-o, строгая верхняя оценка)** — множество функций o(g(n)) = {f(n): для любой положительной константы c > 0 существует n₀ > 0, такое что 0 ≤ f(n) < c·g(n) для всех n ≥ n₀}. lim(n→∞) f(n)/g(n) = 0. Пример: 2n = o(n²), но 2n² ≠ o(n²).

**ω-нотация (Little-omega, строгая нижняя оценка)** — множество функций ω(g(n)) = {f(n): для любой положительной константы c > 0 существует n₀ > 0, такое что 0 ≤ c·g(n) < f(n) для всех n ≥ n₀}. lim(n→∞) f(n)/g(n) = ∞. Пример: n²/2 = ω(n), но n²/2 ≠ ω(n²).

**Аналогия с операторами сравнения**:
- f(n) = O(g(n)) подобно a ≤ b
- f(n) = Ω(g(n)) подобно a ≥ b
- f(n) = Θ(g(n)) подобно a = b
- f(n) = o(g(n)) подобно a < b
- f(n) = ω(g(n)) подобно a > b

**Временная сложность (Time Complexity)** — характеристика масштабирования времени обработки.

**Пространственная сложность (Space Complexity)** — объём памяти, требуемый алгоритму. Если создаётся массив размера n — это O(n) памяти. Включает память, выделяемую под вызовы в стеке рекурсии.

**Амортизированное время (Amortized Time)** — среднее время выполнения одной операции в наихудшем случае, усреднённое по последовательности из n операций. Пример: вставка в динамический массив (ArrayList) стоит O(1) амортизированно, хотя расширение стоит O(n).

**BCR (Best Conceivable Runtime)** — абсолютно наилучшее теоретически возможное время выполнения для задачи. Не связано с "Best Case". Работа ниже BCR считается «бесплатной» (freebie) — например, если BCR = O(N), создание хэш-таблицы за O(N) не ухудшит итоговую оценку.

**BUD** — подход к оптимизации: Bottlenecks (узкие места — самая медленная часть кода, тормозящая всё остальное), Unnecessary work (лишняя работа), Duplicated work (дублирование).

## Классы сложности

| Нотация | Название | Пример |
|---------|----------|--------|
| O(1) | Константное | Доступ к элементу массива по индексу |
| O(log n) | Логарифмическое | Бинарный поиск (задача делится пополам на каждом шаге) |
| O(n) | Линейное | Простой линейный поиск |
| O(n log n) | Квазилинейное | Mergesort, Quicksort (в среднем) |
| O(n²) | Квадратичное | Два вложенных цикла, selection sort |
| O(2ⁿ) | Экспоненциальное | Рекурсивный Фибоначчи (время удваивается с каждым элементом) |
| O(n!) | Факториальное | Перебор всех перестановок (Задача коммивояжёра) |

## Как устроено

### Правила расчёта

**1. Отбрасывание констант**
O(2N) → O(N). Big-O описывает скорость роста, а не точное число операций.

**2. Отбрасывание незначительных членов**
O(N² + N) → O(N²). При больших N меньший член становится несущественным.

**3. Алгоритмы из нескольких частей: сложение vs умножение**
- **Сложение**: если алгоритм делает "A единиц работы, затем B единиц работы" → O(A + B)
- **Умножение**: если алгоритм делает "B единиц работы для каждого элемента из A" → O(A × B)

**4. Рекурсивные алгоритмы**
Когда рекурсивная функция делает несколько вызовов, время часто выглядит как:

```
O(ветки^глубина)
```

где `ветки` — количество ветвлений каждого вызова.

### Наилучший, наихудший, средний случай

| Случай | Описание | Применимость |
|--------|----------|--------------|
| **Worst Case** | Максимальное время; даёт гарантию | Big-O обычно описывает этот случай |
| **Expected (Average) Case** | Типичная производительность на случайных данных | Например, Quicksort — O(n log n) в среднем |
| **Best Case** | Минимальное время для «удачного» ввода | Бесполезная метрика на интервью — легко подделать |

### Пространственная сложность

Стековые вызовы рекурсии тоже занимают память. При глубине рекурсии n — это O(n) памяти, так как каждый вызов остаётся в стеке до завершения.

## Детали и нюансы

**Переменные для разных входов**: если в задаче два массива длин A и B — сложность может быть O(A × B), а не O(N²). Использование одной переменной N для разных входов — распространённая ошибка.

**Строки и хэш-функции**: технически поиск строки в хэш-таблице занимает O(K), где K — длина строки, так как нужно прочитать все символы.

**Амортизированная сложность**: вставка с редким расширением массива (resizing) — O(1) амортизированно. Сумма копирований при удвоении: 1 + 2 + 4 + ... + X/2 не превысит X.

**Методы амортизированного анализа** (CLRS):
- **Aggregate analysis (агрегированный анализ)**: показывается, что последовательность из n операций суммарно занимает T(n) времени. Амортизированная стоимость каждой операции = T(n)/n.
- **Accounting method (метод бухгалтерского учета)**: разным типам операций назначается искусственная "амортизированная" стоимость. Если амортизированная стоимость > фактической, разница сохраняется как "кредит". Суммарный кредит никогда не должен становиться отрицательным.
- **Potential method (метод потенциалов)**: кредит хранится как "потенциальная энергия" Φ(Dᵢ) всей структуры данных. Амортизированная стоимость i-й операции: ĉᵢ = cᵢ + Φ(Dᵢ) − Φ(Dᵢ₋₁).

**Константы имеют значение в реальности**: хотя Big-O отбрасывает константы, на практике меньшая константа может сделать O(n log n) алгоритм с Quicksort быстрее другого O(n log n) с Mergesort.

**Основание логарифма vs основание экспоненты**: основание логарифма отбрасывается в O (так как log₂N = log₁₀N / log₁₀2 — разница на константу). Основание экспоненты отбрасывать ЗАПРЕЩЕНО (O(2ᴺ) ≠ O(8ᴺ) — различаются на множитель 2²ᴺ, что не является константой).

**Определение N в 2D массивах**: если матрица квадратная, O(N) может означать "относительно длины стороны" или "относительно площади/количества элементов". Запрещено использовать N при неопределённости — вместо этого использовать O(W·H).

**Сортировка массива строк**: если длина строки S, размер массива A — сортировка массива строк занимает O(A·S·(logA + logS)), так как каждое сравнение строк стоит O(S).

**Неоднозначность переменной n**: важно уточнять — n означает значение числа или количество битов в нём.

**Закон трихотомии не применим ко всем функциям**: функции n и n^(1+sin(n)) асимптотически несравнимы, так как показатель степени осциллирует между 0 и 2.

## Сравнения и trade-offs

| Подход | Big-O время | Когда применять |
|--------|-------------|-----------------|
| Brute force с вложенными циклами | O(n²) | Отправная точка, потом оптимизируем |
| HashMap для поиска пар | O(n) | Когда нужно убрать внешний цикл |
| Сортировка + два указателя | O(n log n) | Когда данные не отсортированы |
| Бинарный поиск | O(log n) | Только на отсортированных данных |

## Примеры

```go
// O(1) — константное время
func getFirst(arr []int) int {
    return arr[0]
}

// O(n) — линейное время
func linearSearch(arr []int, target int) int {
    for i, v := range arr {
        if v == target {
            return i
        }
    }
    return -1
}

// O(log n) — логарифмическое (бинарный поиск)
func binarySearch(arr []int, target int) int {
    lo, hi := 0, len(arr)-1
    for lo <= hi {
        mid := (lo + hi) / 2
        if arr[mid] == target {
            return mid
        } else if arr[mid] < target {
            lo = mid + 1
        } else {
            hi = mid - 1
        }
    }
    return -1
}

// O(n²) — два вложенных цикла
func hasDuplicateBrute(arr []int) bool {
    for i := 0; i < len(arr); i++ {
        for j := i + 1; j < len(arr); j++ {
            if arr[i] == arr[j] {
                return true
            }
        }
    }
    return false
}

// O(n) — оптимизация через HashMap
func hasDuplicateOptimal(arr []int) bool {
    seen := make(map[int]bool)
    for _, v := range arr {
        if seen[v] {
            return true
        }
        seen[v] = true
    }
    return false
}

// O(2^n) — рекурсивный Фибоначчи
func fib(n int) int {
    if n <= 1 {
        return n
    }
    return fib(n-1) + fib(n-2)
}

// O(n) — Фибоначчи с мемоизацией
func fibMemo(n int, memo map[int]int) int {
    if n <= 1 {
        return n
    }
    if v, ok := memo[n]; ok {
        return v
    }
    memo[n] = fibMemo(n-1, memo) + fibMemo(n-2, memo)
    return memo[n]
}
```

### Сложение vs умножение частей алгоритма

```go
// O(A + B) — сначала A, потом B
func addExample(arrA, arrB []int) {
    for _, v := range arrA { // O(A)
        _ = v
    }
    for _, v := range arrB { // O(B)
        _ = v
    }
}

// O(A * B) — B для каждого элемента A
func multiplyExample(arrA, arrB []int) {
    for _, a := range arrA {   // O(A)
        for _, b := range arrB { // O(B) для каждого
            _, _ = a, b
        }
    }
}
```

## Связи с другими темами

- [[15. Two Pointers]] — часто снижает O(n²) до O(n)
- [[16. Sliding Window]] — снижает O(n²) до O(n) для подмассивов
- [[06. Heap]] — Build-Heap O(n) — классический пример нетривиального анализа; поиск в сбалансированном BST — O(log n)
- [[05. HashMap]] — ключевой инструмент оптимизации до O(n)
- [[19. Dynamic Programming]] — мемоизация снижает экспоненциальное до полиномиального

## Важно / подводные камни / best practices

- **На интервью**: сначала озвучь brute force и его Big-O, потом оптимизируй
- **Не угадывай** сложность — выводи её через логику алгоритма
- **Используй BCR**: пойми теоретический минимум и стремись к нему
- **Применяй BUD**: ищи узкие места, лишнюю и дублирующуюся работу
- **Разные переменные**: не называй всё `n` — разные входы требуют разных обозначений

---

## Карточки для повторения

#flashcards/algorithms/big-o

Что такое Big-O?::Метрика описывающая скорость роста числа операций алгоритма относительно размера входных данных n. Описывает верхнюю границу (worst case).

O(1) — что это и пример?::Константное время. Скорость не зависит от размера входа. Пример: доступ к элементу массива по индексу.

O(log n) — что это и пример?::Логарифмическое время. Задача делится пополам на каждом шаге. Пример: бинарный поиск.

O(n) — что это и пример?::Линейное время. Скорость растёт пропорционально входу. Пример: линейный поиск по массиву.

O(n log n) — что это и пример?::Квазилинейное время. Характерно для эффективных алгоритмов сортировки: Mergesort, Quicksort (в среднем).

O(n²) — что это и пример?::Квадратичное время. Два вложенных цикла. Пример: selection sort, brute force поиск пары.

O(2^n) — что это и пример?::Экспоненциальное время. Время удваивается с каждым новым элементом. Пример: рекурсивный Фибоначчи без мемоизации.

O(n!) — что это и пример?::Факториальное время. Перебор всех перестановок. Пример: решение Задачи коммивояжёра.

Правило отбрасывания констант::O(2N) → O(N). Big-O описывает скорость роста, константы несущественны.

Правило отбрасывания незначительных членов::O(N² + N) → O(N²). При больших N меньший член становится несущественным.

Когда складывать, а когда умножать сложности?::Складывать — если алгоритм делает A работы, ЗАТЕМ B. Умножать — если делает B работы ДЛЯ КАЖДОГО элемента из A.

Формула сложности рекурсивного алгоритма::O(ветки^глубина), где ветки — количество рекурсивных вызовов на каждом уровне.

Чем отличаются Worst/Expected/Best Case?::Worst — максимум (Big-O); Expected — на случайных данных; Best — минимум для «удачного» ввода (бесполезен на интервью).

Что такое BCR?::Best Conceivable Runtime — теоретически наилучшее возможное время для задачи. Работа ниже BCR считается «бесплатной».

Что такое BUD?::Подход к оптимизации: Bottlenecks (узкие места), Unnecessary work (лишняя работа), Duplicated work (дублирование).

Как рекурсия влияет на пространственную сложность?::Каждый рекурсивный вызов занимает место в стеке. При глубине рекурсии n → O(n) памяти.

Типичная ошибка с несколькими входами::Использовать одну переменную N для разных массивов. Если массивы длин A и B — сложность O(A × B), а не O(N²).

Θ-нотация (Big Theta):::Асимптотически точная оценка. f(n) = Θ(g(n)) означает c₁·g(n) ≤ f(n) ≤ c₂·g(n). Аналогично a = b.

Ω-нотация (Big Omega):::Асимптотическая нижняя оценка. f(n) = Ω(g(n)) означает c·g(n) ≤ f(n). Аналогично a ≥ b.

o-нотация (Little-o):::Строгая верхняя оценка. lim(n→∞) f(n)/g(n) = 0. Аналогично a < b. Пример: 2n = o(n²).

ω-нотация (Little-omega):::Строгая нижняя оценка. lim(n→∞) f(n)/g(n) = ∞. Аналогично a > b.

Три метода амортизированного анализа?
?
1. Aggregate analysis — суммарное T(n) / n
2. Accounting method — искусственные стоимости + кредиты на объектах
3. Potential method — потенциальная энергия Φ(Dᵢ), ĉᵢ = cᵢ + Φ(Dᵢ) − Φ(Dᵢ₋₁)

Почему основание логарифма отбрасывается, а основание экспоненты — нет?::Логарифмы разных оснований отличаются на константу (log₂N = log₁₀N / log₁₀2). Экспоненты — нет: O(2ᴺ) ≠ O(8ᴺ), разница 2²ᴺ — не константа.

Сложность сортировки массива строк (длина строки S, размер массива A)?::O(A·S·(logA + logS)). Каждое сравнение строк стоит O(S).

---

## Источники

**Книга:**
- Название: Grokking Algorithms
- Автор: Aditya Bhargava (Адитья Бхаргава)
- Год: 2016 (оригинал, Manning Publications Co.) / 2017 (русское издание, «Питер»)
- ISBN: 978-1-617292231 (EN) / 978-5-496-02541-6 (RU)
- Переводчик (RU): Е. Матвеев

**Книга:**
- Название: Cracking the Coding Interview (6th Edition)
- Автор: Gayle Laakmann McDowell
- Год: 2015 (copyright), 2016 (компиляция)
- Издатель: CareerCup, LLC (Palo Alto, CA)
- ISBN: 978-0-9847828-5-7

**Книга:**
- Название: Introduction to Algorithms (3rd Edition) — CLRS
- Авторы: Thomas H. Cormen, Charles E. Leiserson, Ronald L. Rivest, Clifford Stein
- Год: 2009
- Издатель: The MIT Press (Cambridge, Massachusetts / London, England)
- ISBN: 978-0-262-03384-8 (hardcover) / 978-0-262-53305-8 (paperback)

---

## Теги

#конспект #algorithms #big-o #complexity #interview-prep
