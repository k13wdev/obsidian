## Суть и контекст

B-Tree (B-дерево) — основная структура данных для индексов в реляционных СУБД. Это сбалансированное дерево, оптимизированное для дисковых операций: каждый узел соответствует дисковой странице, а глубина дерева обычно составляет 3–4 уровня даже для сотен миллионов записей.

---

## Ключевые идеи

- B-Tree — направленный ациклический граф (сбалансированное дерево), узел которого представляет собой список пар «ключ—значение», перемежающихся ссылками на поддеревья, где все ключи уникальны и упорядочены.
- Каждый узел размещается в выделенной дисковой странице (обычно 8 КБ).
- Глубина дерева = логарифм по основанию N от количества ключей. При размере указателя 8 байт, ключа 30 байт и страницы 8 КБ, на 4-м уровне размещается до 8 000 000 страниц (~60 ГБ, >300 млн объектов).
- **B+Tree** — модификация, в которой значения содержатся только в листовых узлах, а листья образуют **двунаправленный список** для эффективных range-запросов.
- **B*Tree** — модификация, при переполнении узла пытающаяся перенести часть ключей в соседний узел вместо безусловного разделения, что увеличивает плотность хранения.

---

## Определения и термины

**B-Tree (B-дерево)** — сбалансированное сильноветвистое дерево поиска, где данные упорядочены. Организовано как иерархия страниц; каждый узел — дисковая страница (обычно 8 КБ). Обеспечивает поиск, вставку и удаление за O(log N).

**B+Tree (B+-дерево)** — модификация B-дерева, в которой значения (TID) содержатся только в листовых узлах (в промежуточных — только ключи-разделители), а листовые узлы образуют двунаправленный список для эффективных range-запросов.

**B*Tree** — модификация B-дерева, алгоритм которой при переполнении узла пытается перенести часть ключей в соседний узел вместо безусловного разделения, что увеличивает плотность хранения данных.

**Метастраница (Metapage)** — нулевая страница B-Tree индекса, хранящая номер корневой страницы и управляющую информацию.

**High Key (Верхний ключ)** — дополнительный ключ на каждой листовой странице (кроме самой правой), служащий верхней границей для всех значений на ней.

**Fill Factor (Коэффициент заполнения)** — процент заполнения страницы при создании индекса. Оставшееся место резервируется для будущих вставок, предотвращая преждевременные splits.

**Fanout (Коэффициент ветвления)** — количество дочерних указателей в одном узле: N+1, где N — число ключей.

---

## Как устроено

### Структура узлов

- **Корневой узел (Root)** — верхний уровень дерева.
- **Промежуточные узлы** — содержат ключи и ссылки на поддеревья. Ключи в поддереве, на которое ссылается указатель между K_n и K_{n+1}, лежат в промежутке [K_n; K_{n+1}).
- **Листовые узлы (Leaf)** — содержат ключи и физические адреса строк (TID). В B+Tree ключи могут дублироваться в дочерних узлах для маршрутизации.
- Количество потомков у каждого узла на единицу больше, чем N (количество ключей, помещающихся в дисковую страницу).

### Алгоритм поиска

1. Чтение корневой страницы.
2. Сравнение искомого значения с ключами для выбора интервала и соответствующего указателя на дочерний узел.
3. Спуск по дереву до достижения листового узла (Leaf).
4. Поиск ключа в листовом узле и извлечение физического адреса (номера строки).
5. **В B+Tree** для range-запросов — после нахождения первого листа горизонтальное перемещение по двунаправленному списку листовых узлов.

### Алгоритм вставки и расщепления (Splitting)

1. Спуск до листового узла, в который должен попасть ключ.
2. Если узел заполнен менее максимума (с учётом FILLFACTOR), ключ вставляется с сохранением сортировки.
3. Если узел заполнен — **расщепление**: он делится пополам; половина ключей остаётся, половина переносится в новый узел.
4. Разделяющий ключ поднимается в узел-предок, обновляются границы указателей.
5. Если предок также переполнен, деление **каскадно** доходит до корня. При делении корня создаётся новый корневой узел с единственным ключом, и глубина дерева увеличивается на 1.

### Алгоритм удаления и балансировки

1. Удаление ключа из листового узла.
2. Если заполнение узла падает ниже допустимого порога (менее 50% для B-Tree, менее 2/3 для B*Tree), инициируется слияние.
3. Алгоритм пытается объединить узел с соседним.
4. Разделяющий ключ удаляется из узла-предка. Процесс может каскадно дойти до корня.

### Алгоритм поиска (детально)

1. Считать номер корневой страницы из **метастраницы**.
2. В текущем узле найти ключ K_i, удовлетворяющий: K_i ≤ искомое значение < K_{i+1}.
3. Перейти по ссылке на дочернюю страницу. Повторять до достижения листа.
4. В листовой странице — двоичный поиск точного значения, извлечение TID.
5. Для range-запросов (B+Tree): после нахождения первого листа — горизонтальный обход по двунаправленному списку листовых страниц.

### Блокировки

При доступе к страницам B-Tree используются **кратковременные блокировки (LWLock / latches)**:
- Захватываются в разделяемом режиме для чтения и в исключительном — для вставки.
- Отпускаются сразу после обработки страницы — не удерживаются до конца транзакции.
- Обеспечивают высокую параллельность; исключают deadlocks на уровне индекса.

---

## Детали и нюансы

- **Монотонно возрастающие ключи (автоинкремент)**: массированная вставка направляется исключительно в крайний правый листовой узел → высокая конкуренция за одну страницу → падение производительности. Решение: индекс по инвертированному представлению ключа (REVERSE в Oracle), равномерно размазывающий вставки, ценой потери эффективности range-запросов.
- **PCTFREE / FILLFACTOR**: оставляет резерв пустого места в страницах при начальном построении; при последующих вставках узлы не будут немедленно расщепляться.
- **Глубина 3–4 уровня** покрывает сотни миллионов объектов — на практике верхние уровни кэшируются в памяти.
- **Raздувание (Bloat)**: B-Tree растёт при вставках, но не уменьшается автоматически при удалениях. Мёртвые записи помечаются, страницы остаются за индексом. Для устранения — REINDEX или VACUUM FULL.
- **WAL и Durability**: все изменения B-Tree фиксируются в WAL. При первом изменении страницы после checkpoint в WAL записывается образ всей страницы (**full_page_writes**) для защиты от torn pages при сбое.

### Оптимизации PostgreSQL (новые версии)

- **Deduplication (PG 13+)**: схлопывание дублирующихся ключей в одну запись со списком TID. Экономит место и замедляет расщепление страниц.
- **Suffix Truncation (PG 12+)**: во внутренних узлах хранятся только минимально необходимые части ключей (префиксы) для маршрутизации. Увеличивает fanout.
- **Bottom-up index deletion (PG 14+)**: очистка индекса от мёртвых версий прямо в момент вставки при переполнении страницы — предотвращает лишние splits.

---

## Сравнения и trade-offs

| Вариант | Плюсы | Минусы | Когда использовать |
|---------|-------|--------|--------------------|
| B-Tree | Логарифмический поиск O(log_N K), поддержка порядка, range-запросы | Медленнее O(1) для точного поиска | По умолчанию для большинства запросов |
| B+Tree | Эффективные range-запросы (двунаправленный список листьев) | Дублирование ключей в промежуточных узлах | PostgreSQL, MySQL (InnoDB) |
| B*Tree | Выше плотность хранения (перенос ключей вместо расщепления) | Сложнее алгоритм | Когда критична плотность |

---

## Примеры

**Визуализация поиска числа 61**: корневой блок (ROOT) с диапазонами → переход по ссылке 50–100 → промежуточный блок → переход по ссылке 50–65 → листовой узел → ключ 61, физический номер строки 10.

---

## Связи с другими темами

- [[03. Индексы и хранение данных]] — B-Tree как метод доступа по умолчанию в PostgreSQL; сравнение с Hash индексом.
- [[05. Hash таблица]] — альтернатива B-Tree для операций точного равенства (O(1) vs O(log N)).
- [[09. Внутреннее устройство PostgreSQL]] — планировщик выбирает Index Scan по B-Tree на основе селективности.
- [[07. Оптимизация запросов]] — FILLFACTOR и структура индекса влияют на производительность.

---

## Важно / подводные камни / best practices

- **Используйте FILLFACTOR** для таблиц с частым обновлением — предотвращает преждевременное расщепление узлов.
- **Осторожно с автоинкрементными ключами** в высоконагруженных системах — hot spot на правом листе.
- **B-Tree page-level locks кратковременны** — безопаснее для конкурентного доступа, чем Hash.

---

## Карточки для повторения

#flashcards/db/btree

B-Tree (B-дерево):::Направленный ациклический граф (сбалансированное дерево), узел которого — список пар «ключ—значение», перемежающихся ссылками на поддеревья; все ключи уникальны и упорядочены

B+Tree (B+-дерево):::Модификация B-дерева, в которой значения содержатся только в листовых узлах, а листовые узлы образуют двунаправленный список

Чем B*Tree отличается от B-Tree?::При переполнении узла B*Tree пытается перенести часть ключей в соседний узел вместо безусловного разделения, увеличивая плотность хранения

Алгоритм поиска в B-Tree
?
1. Чтение корневой страницы
2. Сравнение ключей, выбор указателя на интервал
3. Спуск по дереву до листового узла
4. Поиск ключа в листе, извлечение физического адреса строки

Алгоритм вставки и расщепления в B-Tree
?
1. Спуск до целевого листового узла
2. Если есть место — вставка с сохранением сортировки
3. Если узел полон — расщепление пополам, новый узел
4. Разделяющий ключ поднимается в предка
5. Каскадное деление до корня при необходимости

Что происходит при расщеплении корневого узла B-Tree?::Создаётся новый корневой узел с единственным ключом, глубина дерева увеличивается на 1
<!--SR:!2026-04-16,1,230-->

Глубина B-Tree обычно составляет =={3–4 уровня}== даже для сотен миллионов записей

Каждый узел B-Tree размещается в выделенной ==дисковой странице (обычно 8 КБ)==

Почему автоинкрементный PK создаёт проблему для B-Tree?::Все вставки направляются в крайний правый листовой узел → высокая конкуренция за одну страницу → падение производительности записи

Зачем нужен FILLFACTOR/PCTFREE в B-Tree?::Оставляет резерв пустого места в страницах при построении индекса; при последующих вставках узлы не расщепляются немедленно, снижая I/O нагрузку
<!--SR:!2026-04-16,1,230-->

Сложность поиска в B-Tree ==O(log_N K)==, где N — количество ключей в странице

Метастраница (Metapage) B-Tree:::Нулевая страница индекса, хранящая номер корневой страницы и управляющую информацию

High Key (Верхний ключ):::Дополнительный ключ на листовой странице (кроме самой правой) — верхняя граница всех значений на ней

Fill Factor (Коэффициент заполнения):::Процент заполнения страницы при создании индекса; оставшееся место резервируется для вставок, предотвращая преждевременные splits

Как создать B-Tree индекс с Fill Factor в PostgreSQL?::`CREATE INDEX ON table (column) WITH (fillfactor = 75);`

Deduplication в B-Tree (PG 13+):::Схлопывание дублирующихся ключей в одну запись со списком TID. Экономит место и уменьшает splits.

Suffix Truncation в B-Tree (PG 12+):::Внутренние узлы хранят только минимально необходимые части ключей (префиксы) для маршрутизации. Увеличивает fanout.

Bottom-up index deletion (PG 14+):::Очистка индекса от мёртвых версий прямо при вставке в переполненную страницу — предотвращает лишние splits.

Какие блокировки использует B-Tree?::LWLock (latches) — кратковременные блокировки на уровне страниц; отпускаются сразу после обработки страницы; не удерживаются до конца транзакции

Зачем в WAL пишется образ всей страницы B-Tree после checkpoint?::full_page_writes: защита от torn pages (частичной записи) при сбое — позволяет корректно восстановить страницу при crash recovery

---

## Источники

**Видео:**
- Название: Database 15.mp4
- Содержание: устройство B-Tree, поиск, вставка, расщепление узлов
- Таймкоды: в источниках не указаны

**Документация:**
- Название: PostgreSQL 18.3 Documentation
- Автор: The PostgreSQL Global Development Group
- Ключевые разделы: 11.2 Index Types (B-Tree)

**Книга:**
- Название: «Путеводитель по базам данных»
- Автор: Владимир Комаров / Postgres Professional
- Ключевые разделы: 3.2 Изменяемые структуры (B-дерево)

**Книга:**
- Название: «PostgreSQL 17 изнутри»
- Автор: Егор Рогов
- Год: 2024
- Разделы: Глава 25 (B-дерево: metapage, High Key, deduplication, suffix truncation, bottom-up deletion)
- Ссылка: edu.postgrespro.ru/postgresql_internals-17.pdf

**Книга:**
- Название: «Введение в системы баз данных» (8-е издание)
- Автор: C.J. Date
- Ключевые разделы: Приложение Г.4 (B-деревья)

---

## Теги

#конспект #database #b-tree #index #data-structure
